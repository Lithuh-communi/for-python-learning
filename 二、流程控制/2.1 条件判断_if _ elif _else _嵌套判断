条件判断让程序能根据条件真假选择不同的执行路径。
Python 用 if / elif / else 实现，核心特征是靠缩进（4空格）定义代码块，
没有 switch 或 case（3.10+ 有 match）
————————————————————————————————————————————————————————————————————————
1️⃣ 基本结构：if
  if 条件:
     条件为真时执行的代码
 规则：条件为 True 或可转换为真的值时，执行缩进块；否则跳过。

例子：
age = 18
if age >= 18:
    print("你已成年")   # 输出

图解：
┌─────────────────────────────────────┐
│  age = 18                           │
│                                     │
│  if age >= 18:       ← 条件判断      │
│  ─────────────────                  │
│  │  print("你已成年")  ← 真值块      │
│                                     │
│  → 18 >= 18 → True → 进入块 ✅      │
└─────────────────────────────────────┘
————————————————————————————————————————————————————————————————————————————-——
2️⃣ 二选一：if / else
if 条件:
    真值块
else:
    假值块

例子：
score = 59
if score >= 60:
    print("及格")
else:
    print("不及格")     # 输出

图解：
        ┌─────────────┐
        │  score = 59  │
        └──────┬──────┘
        ┌──────▼──────┐
        │ score >= 60? │
        └──┬───────┬──┘
      False│       │True
           │       │
    ┌──────▼──┐  ┌─▼───────┐
    │不及格    │  │及格      │
    └─────────┘  └─────────┘

Python 特有语法：条件表达式（三元运算符）:
result = "及格" if score >= 60 else "不及格"
适用于简单二选一，复杂逻辑请用完整 if/else
————————————————————————————————————————————————————————————————————————————————————
3️⃣ 多分支：if / elif / else
if 条件1:
    块1
elif 条件2:
    块2
elif 条件3:
    块3
else:
    兜底块

关键特性：依次检查，只进入第一个满足条件的分支，后续全部跳过

例子：
score = 85

if score >= 90:
    grade = "A"
elif score >= 80:
    grade = "B"          # ← 命中这个，后面的不看
elif score >= 70:
    grade = "C"
elif score >= 60:
    grade = "D"
else:
    grade = "F"

print(grade)             # B

常见坑：顺序很重要！❌ 错误写法：
if score >= 60:
    grade = "D"          # 85 >= 60 → True，先进来就结束了！
elif score >= 80:
    grade = "B"          # 永远不会执行

正确顺序：从最严格到最宽松
——————————————————————————————————————————————————————————————————————————————
4️⃣ 嵌套判断
条件内部再套条件，形成多层决策树
例子：
age = 20
has_id = True

if age >= 18:
    if has_id:
        print("允许入场")    # 输出
    else:
        print("请出示证件")
else:
    print("未成年禁止入内")

更实际的例子：用户登录验证：
username = "admin"
password = "123456"

if username == "admin":          # 第一层：用户名
    if password == "123456":     # 第二层：密码
        print("登录成功")
    else:
        print("密码错误")
else:
    print("用户不存在")

————————————————————————————————————————————————————————————————————————————————
5️⃣ 逻辑运算符组合条件
用 and / or / not 把多个条件写在一行，避免深度嵌套

例子：
age = 20
has_id = True

# 用 and 代替嵌套
if age >= 18 and has_id:
    print("允许入场")
else:
    print("禁止入场")

短路求值：and 遇到第一个 False 就停，or 遇到第一个 True 就停：
def check():
    print("check 被调用了")
    return True

if False and check():   # False 已经决定结果，check() 根本不会执行
    pass

if True or check():     # True 已经决定结果，check() 根本不会执行
    pass

————————————————————————————————————————————————————————————————————————
6️⃣ pass — 空占位
Python 语法要求代码块不能为空，有时需要占位：
if condition:
    pass          # 啥也不做，后面再写
else:
    do_something()

_______________________
┌────────────────────────────────────────────────────────────┐
│  📋 Python 条件判断速查卡                                   │
├────────────────────────────────────────────────────────────┤
│                                                            
│  🔹 基本:  if 条件:    (冒号不能少！)                        
│            ┊ 代码块    (缩进4空格)                           
│                                                            
│  🔹 二选一: if 条件: ... else: ...                          
│                                                            
│  🔹 多分支: if 条件: ... elif 条件: ... else: ...           
│             依次检查，只进第一个匹配                          
│                                                            
│  🔹 嵌套:   if 外层:                                       
│                if 内层: ...                                
│                                                            
│  🔹 组合:   and(与)/or(或)/not(非)                          
│             短路求值：and遇False停，or遇True停               
│                                                            
│  🔹 三目:   x if 条件 else y                               
│                                                            
│  🔹 真假:   False: 0, "", [], {}, None                     
│             True:  非零数, 非空串/容器                      
│                                                           
│  ⚠️ 常见错误:                                              
│   · 忘记冒号 → SyntaxError                                 
│   · 缩进不一致 → IndentationError                          
│   · = 写成 == → 赋值而非比较（不会报错但逻辑错）            
│   · elif 写成 else if → 语法错误（Python不能这样写）        
│   · 顺序错误 → 永远进不了后面的分支                         
│                                                            
└────────────────────────────────────────────────────────────

要点	                             说明
缩进即块	          Python 靠缩进（4空格）区分代码块，没有 {},不像C语言
冒号不能漏	          if 条件: 冒号是固定语法
elif 不是 else if	  Python 特有写法，其他语言通常是 else if
顺序决定命运	      多分支要按"最严格→最宽松"排序
嵌套不超过3层	      超过3层考虑用 and/or 或拆函数
== 比较，= 赋值	      写错不会报错但逻辑全歪